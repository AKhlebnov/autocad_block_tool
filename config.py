# config.py – файл с настройками для скрипта маркировки камер

# Имя блока, который нужно обрабатывать
# BLOCK_NAME = "camera"   # замени на своё, если нужно
# BLOCK_NAME = "WiFi"   # замени на своё, если нужно
BLOCK_NAME = "SOCKET_1P"   # замени на своё, если нужно

# Соответствие логических полей тегам атрибутов в блоке
# Здесь мы указываем, какой атрибут использовать для какой цели
TAG_CABINET = "MAC"      # временно храним номер шкафа в атрибуте MAC
TAG_FLOOR = "IP"         # временно храним номер этажа в атрибуте IP
TAG_NAME = "NAME"        # сюда будем записывать итоговое имя
# Эти атрибуты мы НЕ будем трогать при импорте (можно оставить для справки)
TAG_ICON = "ICON"
TAG_SERIAL = "SERIAL_NUMBER"

# Диапазоны портов для одной панели (список списков)
# PORT_RANGES = [list(range(1, 19)), list(range(25, 43))]  # 1-18 и 25-42 для камер
# PORT_RANGES = [list(range(19, 24)), list(range(43, 48))]  # 19-23 и 43-47 для Wi-Fi
PORT_RANGES = [list(range(1, 24)), list(range(25, 48))]  # 1-23 и 25-47 Все порты, кроме 24 и 48

# Формат итогового имени NAME
# Используются переменные: {cabinet}, {panel}, {port}
NAME_FORMAT = "{cabinet}/{panel:02d}.{port}"

# Список атрибутов, которые нужно очистить после обработки (временные)
TEMP_ATTRIBUTES = [TAG_CABINET, TAG_FLOOR]   # MAC и IP

# (Опционально) Можно задать сортировку: сначала по шкафу, потом по этажу, потом по Y, потом по X
# ascending: True – по возрастанию, False – по убыванию
SORT_BY = [
    TAG_CABINET,   # сначала группируем по шкафу
    TAG_FLOOR,     # потом по этажу
    'Y',           # потом по Y (сверху вниз = убывание)
    'X'            # потом по X (слева направо = возрастание)
]
SORT_ASCENDING = [True, True, False, True]

# Пропуск портов для конкретных шкафов и панелей
# Формат: (шкаф, номер_панели): список_портов_или_диапазонов
# Номер панели – целое число (1,2,3...)
# Диапазон задаётся кортежем (начало, конец) включительно
SKIP_PORTS = {

    ("АБЧ2.1", 1): [(1, 48)],           # Вся первая панель шкафа АБЧ2.1
    ("АБЧ2.1", 2): [(1, 48)],           # Вся вторая панель шкафа АБЧ2.1
    ("АБЧ2.1", 3): [(1, 48)],           # Вся вторая панель шкафа АБЧ2.1
    ("АБЧ2.1", 4): [(1, 48)],           # Вся вторая панель шкафа АБЧ2.1
    ("АБЧ2.1", 5): [(1, 48)],           # Вся вторая панель шкафа АБЧ2.1
    ("АБЧ2.1", 6): [(1, 22)],           # Вся вторая панель шкафа АБЧ2.1
    # ("АБЧ2.1", 1): [(19, 23), (43, 47)],           # первая панель шкафа АБЧ2.1
    # ("АБЧ2.1", 2): [(19, 23), (43, 47)],           # вторая панель шкафа АБЧ2.1
    # ("3.1A1", 1): [(25, 34)],
    #("1-1.B13", 1): [(25, 34)],           # первая панель шкафа B13
    #("1-1.B13", 2): [(1, 5), (40, 42)],   # вторая панель того же шкафа
    #("1-1.A09", 3): [(2, 5), 7, 9],       # третья панель шкафа A09
    #("1-1.A09", 4): [(30, 40)],           # четвёртая панель шкафа A09
    #("1.A3", 1): [(25, 34)],          # пропустить порты 25-34 на первой панели шкафа 1.A3
    #("1.B2", 2): [(25, 30)],          # пропустить порты 25-30 на второй панели шкафа 1.B2
    # Добавляй другие исключения по аналогии
}

# Максимальное количество патч-панелей в одном шкафу (можно увеличить)
MAX_PANELS_PER_CABINET = 20
